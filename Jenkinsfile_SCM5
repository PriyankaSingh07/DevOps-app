pipeline {
 agent {label 'jenkins-slave'}
 options {
  skipDefaultCheckout()
 }

stages {
  stage('SCM') {
   steps {
   bat """
            cd /var/jenkins_home
        """ 
    checkout scm
    sleep 500
   }
  }
}
}
