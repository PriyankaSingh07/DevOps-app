pipeline {
 agent {label 'jenkins-slave'}
 options {
  skipDefaultCheckout()
 }
stages {
    stage('SCM') {
      steps {
        sh "cd /var/jenkins_home"
        echo $PWD
          checkout scm
          sleep 200
      }
    }
  }
}
