pipeline {
 agent {label 'jenkins-slave'}
 options {
  skipDefaultCheckout()
 }
stages {
  stage('SCM') {
   steps {
  dir('/var/jenkins_home'){
  echo $PWD
      sh $PWD
         checkout scm
     }
   }
  }
 }
}
